name: order_list
definition:
  # Wires connect to data in collections
  wires:
    order:
      collection: usio/pay.order
      fields:
        usio/pay.amount:
        usio/pay.product:
        usio/pay.transaction:
      batchsize: 200
      order:
        - desc: true
          field: uesio/core.updatedat
  # Components determine the layout and composition of your view
  components:
    - uesio/io.viewlayout:
        left:
          - usio/pay.nav:
        content:
          - uesio/io.titlebar:
              title: $Collection{order:pluralLabel}
              subtitle: List View
              uesio.variant: usio/pay.main
              avatar:
                - uesio/io.text:
                    uesio.variant: uesio/io.icon
                    text: shopping_cart
          - uesio/io.box:
              uesio.variant: usio/pay.primarysection
              components:
                - uesio/io.toolbar:
                    linkedComponentType: uesio/io.table
                    linkedComponentId: orderTable
                    left:
                      - uesio/io.searchbox:
                          uesio.variant: usio/pay.main
                          wire: order
                          placeholder: Search Orders
                          searchFields:
                            - usio/pay.product
                - uesio/io.table:
                    uesio.variant: usio/pay.main
                    rowactions:
                      - icon: visibility
                        text: View
                        type: DEFAULT
                        uesio.display:
                          - type: recordIsNotNew
                        signals:
                          - signal: route/NAVIGATE_TO_ASSIGNMENT
                            viewtype: detail
                            recordid: ${uesio/core.id}
                            collection: usio/pay.order
                    columns:
                      - field: usio/pay.product
                      - field: usio/pay.amount
                      - field: usio/pay.transaction
                    uesio.id: orderTable
                    mode: READ
                    wire: order
                    pagesize: 10
